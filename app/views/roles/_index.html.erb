<table class="table table-hover">
  <thead>
    <tr>
      <th scope="col">Role Name</th>
      <th scope="col">Created At</th>
      <th scope="col">Actions</th>
    </tr>
  </thead>
  <tbody>
    <% @roles.each do |role| %>
     <tr style="cursor: pointer;" onclick="window.location='<%= role_path(role) %>'">
       <td><%= role&.name %></td>
       <td><%= role&.created_at.strftime('%Y-%m-%d') %></td>
       <td onclick="event.stopPropagation();" class="position-relative">
        <div class="dropdown">
         <button class="btn btn-link text-dark p-0" type="button" data-bs-toggle="dropdown" aria-expanded="false">
          <i class="bi bi-three-dots"></i>
         </button>
         <ul class="dropdown-menu">
          <li>
           <%= button_to role_path(role), method: :delete, data: { confirm: "Are you sure?" }, form: { style: "display:inline;" }, class: "dropdown-item text-black px-3 py-1 text-center", style: "font-size: 0.85rem; width: 100px;" do %>
             Delete
           <% end %>
          </li>
         </ul>
        </div>
       </td>

     </tr>
    <% end %>
  </tbody>
</table>
<div class="mt-3 d-flex justify-content-end">
  <%= paginate @roles %>
</div>